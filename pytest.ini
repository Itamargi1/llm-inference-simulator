[pytest]
# Put the project root on sys.path so `pytest` works the same as `python -m pytest`.
# Without this, the bare `pytest` console script cannot import the `app` package.
pythonpath = .
testpaths = tests
